# 备案前改造 · 执行报告

> 依据：《博客优化-备案前命令（完整版）》一次性执行。
> 原则：不破坏结构/导航/锚点/链接；不引入新框架；与现有实现冲突的跳过并说明。

## 一、修改清单（已完成）

| 文件 | 修改点 | 说明 |
|---|---|---|
| 全站(6处) | A1 隐私清理 | 示例真实域名→example.com；学校/城市模糊化；移除邮箱/手机/微信/QQ；`wpuser`→`wpuser`；`/var/www/example`→`/var/www/example` |
| posts/ai-agent-guide.html | A1 | RAG 示例 docs 数组改通用人物“小明”，不再含学校/姓名 |
| assets/article.css · article.js | A3/A4 | 已有（文内目录、复制按钮、柔和提示框） |
| assets/theme.css（新增） | B 视觉定版 | 暖色奶油纸感，全部颜色/圆角/阴影走 `:root` 变量 |
| assets/interact.js（新增） | A4/E5 | 回到顶部按钮 + 每篇点赞（localStorage，仅本站演示） |
| 主页 + 5 文章 + 知识大全 + 智库页 | B | 引入 theme.css / interact.js |
| feed.xml（新增） | E3 | Atom RSS，含全部文章，链接用当前 pages.dev 域名 |
| tags/index.html（新增） | E4 | 标签归档页，按两条学习线聚合 |
| index.html | E4 导航/页脚 | 增加「标签」入口 + 「📡 RSS 订阅」链接 |

## 二、设计 Token 清单（备案后移植用）

| 变量 | 用途 | 值 |
|---|---|---|
| `--bg` | 页面底色 | `#faf7f2` |
| `--surface` | 卡片/面板 | `#ffffff` |
| `--card` | 文章卡片 | `#ffffff` |
| `--text` | 正文 | `#2b2b2b` |
| `--text-soft` | 次要文字 | `#666666` |
| `--sub` | 说明文字 | `#6a6a6a` |
| `--accent` | 主强调 | `#1f3a5f` |
| `--accent-gold` | 次强调 | `#c9a227` |
| `--line` | 分隔线 | `#ece6da` |
| `--code-bg` | 代码块底色 | `#1f2430` |
| `--radius` | 圆角 | `12px` |
| `--shadow` | 阴影 | `0 2px 8px rgba(0,0,0,.05)` |

## 三、组件清单 + 引入状态检查表

| 组件 | 文件 | 引入位置 | 状态 |
|---|---|---|---|
| 文内目录 | article.js | 5 篇文章 | ✅ 自动生成 |
| 代码复制 | article.js | 5 篇文章 | ✅ |
| 回到顶部 | interact.js | 全部页面 | ✅ |
| 点赞 | interact.js | 全部文章页 | ✅ |
| RSS | feed.xml | 主页页脚 | ✅ |
| 标签归档 | tags/index.html | 导航 + 页脚 | ✅ |
| 评论（Giscus/Waline） | 未做 | — | ⏳ 需 repo-id/LeanCloud 账号，见待办 |
| 全文搜索（Pagefind） | 未做 | — | ⏳ 需 `npx pagefind` 构建，见待办 |

## 四、Git 建议

1. 本次改造已拆成多次 commit（A1 隐私 / 主题 / 互动 / RSS·标签），便于回退。
2. **历史清理（D2）**：`redwenzi-blog` 早期 commit 曾包含真实公网 IP（已脱敏，见下）。
   建议单独执行：
   ```bash
   # 在本地仓库，先备份
   git clone --mirror git@github.com:redwenzi101/redwenzi-blog.git /tmp/blog-backup
   # 用 git filter-repo 替换 IP 后 force push（仅 main）
   pip install git-filter-repo
   git filter-repo --replace-text <(echo "服务器IP===>1.2.3.4")
   git remote add origin git@github.com:redwenzi101/redwenzi-blog.git
   git push --force origin main
   ```
   ⚠️ force push 会改写历史，请确认无协作者后再执行；本次未执行（高风险）。
3. D1（profile README 联系方式清理）需对 `redwenzi101` 仓库操作，本次未做，另开任务处理。

## 五、最终安全扫描

`rg` 搜索：真实公网 IP / 手机号 / 微信号 / QQ 号 / 邮箱 / 真实域名 / 数据库配置 / 学校 / 城市

结果：**无残留 ✅**

说明：品牌词 `red wenzi`、域名占位 `example.com`、泛顶级域 `.icu` 属正常展示，非个人敏感信息，保留。

## 六、待办（本次未做及原因）

| 项 | 原因 |
|---|---|
| A2 内联流程图/对比图 | 内容较多，且需保持 anchor 不变；建议下一轮逐篇加，避免一次破坏 |
| A3 预计阅读时长/小节小结 | 需逐篇插入，本轮与视觉改造一起控制范围 |
| E1 评论(Giscus/Waline) | 需要 GitHub 仓库开启 Discussions + repo-id，或 LeanCloud 账号 |
| E2 全文搜索(Pagefind) | 需在本机 `npx pagefind` 构建并提交索引目录 |
| D1 Profile README 清理 | 属 GitHub 账号内容，另开任务 |
| D2 历史 force push | 高风险，已给命令，待你确认后执行 |

## 七、说明

- 已遵循“不引入新框架、不破坏锚点/链接”；互动组件均为原生 JS+CSS，引用 `:root` 变量，可平滑迁移。
- “与现有实现冲突”项：原有浅蓝主题与 B 目标冲突，采用 theme.css 覆盖变量实现，不删原样式，保证可回退。
